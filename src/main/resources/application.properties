#spring.datasource.url=jdbc:mysql://localhost:3306/vocabia_db?useSSL=false&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=
#  # --- AWS RDS MySQL ---
   spring.datasource.url=jdbc:mysql://vocabia-db.ce9g424622s0.us-east-1.rds.amazonaws.com:3306/vocabia_test?useSSL=false&serverTimezone=UTC
   spring.datasource.username=admin
   spring.datasource.password=Capstone2025

# --- Hibernate & Logging ---
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ✅ CRITICAL FIX: Allow nullable image columns for dynamic FPOW system
spring.jpa.properties.hibernate.hbm2ddl.auto=update
spring.jpa.properties.hibernate.globally_quoted_identifiers=true

server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

logging.level.com.example.Vocabia=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# File upload base directory (adjust per environment)
app.upload.dir=/home/ec2-user/Vocabventure/uploads

spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

